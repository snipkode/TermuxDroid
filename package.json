{
  "name": "termuxdroid-gui-builder",
  "version": "1.0.0",
  "type": "module",
  "description": "GUI Builder for TermuxDroid - Visual drag-and-drop editor",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd IDE/backend && npm run dev",
    "dev:frontend": "cd IDE/frontend && npm run dev",
    "gui": "node bin/gui-builder.js",
    "install:all": "npm install && cd IDE/backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd IDE/backend && npm start",
    "start:frontend": "cd IDE/frontend && npm run build && npm run preview"
  },
  "keywords": ["gui-builder", "termuxdroid", "android", "visual-editor"],
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
